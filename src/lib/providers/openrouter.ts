export async function narrate(payload:unknown){if(!process.env.OPENROUTER_API_KEY)return null;const r=await fetch("https://openrouter.ai/api/v1/chat/completions",{method:"POST",headers:{Authorization:`Bearer ${process.env.OPENROUTER_API_KEY}`,"Content-Type":"application/json","HTTP-Referer":process.env.NEXT_PUBLIC_APP_URL||"http://localhost:3000","X-Title":"WorldSim"},body:JSON.stringify({model:process.env.OPENROUTER_MODEL||"openai/gpt-oss-20b",temperature:.2,max_tokens:700,messages:[{role:"system",content:"You explain exploratory world simulations neutrally. Never call simulation frequency a real-world probability. Separate assumptions, observed simulated changes, uncertainty, and limitations. Do not make political endorsements or recommendations."},{role:"user",content:JSON.stringify(payload)}]})});if(!r.ok)throw new Error(`OpenRouter request failed (${r.status})`);return(await r.json()).choices?.[0]?.message?.content??null}
