"use client";
import{useState}from"react";
import{KeyRound}from"lucide-react";

export type ProviderKeys={openrouter?:string};

export default function ProviderGate({onConnect}:{onConnect:(keys:ProviderKeys)=>void}){const[openrouter,setOpenrouter]=useState(""),[loading,setLoading]=useState(false),[error,setError]=useState("");async function connect(){setLoading(true);setError("");try{const keys={openrouter:openrouter.trim()},response=await fetch("/api/providers/validate",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(keys)}),body=await response.json();if(!response.ok)throw new Error(body.error||"Provider validation failed.");onConnect(keys)}catch(e){setError(e instanceof Error?e.message:"Provider validation failed.")}finally{setLoading(false)}}return <div className="gate-backdrop"><section className="provider-gate" role="dialog" aria-modal="true" aria-labelledby="provider-title"><div className="gate-icon"><KeyRound/></div><p className="eyebrow">PRIVATE SESSION</p><h1 id="provider-title">Connect OpenRouter</h1><p>Your key stays in this tab, travels only to WorldSim&apos;s server, and is forgotten when you refresh. It powers both simulation decisions and narration.</p><label htmlFor="openrouter-key">OpenRouter API key</label><input id="openrouter-key" type="password" autoComplete="off" value={openrouter} onChange={e=>setOpenrouter(e.target.value)} placeholder="sk-or-…"/><a href="https://openrouter.ai/settings/keys" target="_blank" rel="noreferrer">Get an OpenRouter key ↗</a>{error&&<p className="gate-error" role="alert">{error}</p>}<button className="primary" disabled={loading||!openrouter.trim()} onClick={connect}>{loading?"Checking key…":"Connect OpenRouter"}</button><button className="text-button" disabled={loading} onClick={()=>onConnect({})}>Use deterministic mode</button></section></div>}
