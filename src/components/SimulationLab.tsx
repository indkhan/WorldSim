"use client";
import{Activity,GitBranch,Globe2,Info,Share2,Users}from"lucide-react";import{useMemo,useState}from"react";import type{EventFamily,SimulationResult}from"@/lib/simulation/types";import WorldMap from"./WorldMap";import{ScenarioControls}from"./simulation/ScenarioControls";import{CountryInspector}from"./simulation/CountryInspector";import{ComparisonPanel,WorldSummary}from"./simulation/AnalysisPanels";
type Tab="world"|"country"|"compare";
export default function SimulationLab({initial}:{initial:SimulationResult}){const[result,setResult]=useState(initial),[baseline,setBaseline]=useState<SimulationResult|null>(null),[week,setWeek]=useState(0),[code,setCode]=useState<string|null>(null),[tab,setTab]=useState<Tab>("world"),[description,setDescription]=useState(initial.scenario.description),[family,setFamily]=useState<EventFamily>("energy"),[severity,setSeverity]=useState(65),[duration,setDuration]=useState(12),[loading,setLoading]=useState(false),[error,setError]=useState<string|null>(null);const frame=result.frames[Math.min(week,result.frames.length-1)],selected=frame.countries.find(c=>c.code===code)||null,top=useMemo(()=>[...frame.countries].sort((a,b)=>b.pressure-a.pressure).slice(0,10),[frame]);async function request(sev:number,title:string,desc:string){const r=await fetch("/api/simulations",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({title,description:desc,family,severity:sev,durationWeeks:duration,seed:42})});if(!r.ok)throw new Error((await r.json()).error);return r.json() as Promise<SimulationResult>}async function execute(task:()=>Promise<void>){setLoading(true);setError(null);try{await task()}catch(e){setError(e instanceof Error?e.message:"Something went wrong")}finally{setLoading(false)}}const run=()=>execute(async()=>{setResult(await request(severity,"Custom world event",description));setWeek(0);setCode(null);setTab("world")}),compare=()=>execute(async()=>{setBaseline(await request(0,"No-shock baseline","Baseline continuation without introduced shock."));setTab("compare")}),branch=()=>execute(async()=>{if(!result.id)throw new Error("Configure DATABASE_URL and run a persisted simulation before branching.");const r=await fetch(`/api/simulations/${result.id}/branch`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({description,severity,branchWeek:week,family,durationWeeks:duration})});if(!r.ok)throw new Error((await r.json()).error);setResult(await r.json());setTab("world")});const share=()=>navigator.clipboard?.writeText(location.href),select=(c:{code:string})=>{setCode(c.code);setTab("country")};return <main className="shell"><header className="topbar"><div className="brand"><span className="brandmark"><Globe2/></span><div><b>WORLDSIM</b><small>GLOBAL DECISION SIMULATOR · V1</small></div></div><nav><a href="/methodology"><Info size={14}/>Methodology</a><button onClick={share}><Share2 size={14}/>Share</button></nav></header>{error&&<div className="error-banner">{error}</div>}<div className="layout"><ScenarioControls description={description} family={family} severity={severity} duration={duration} week={week} loading={loading} onDescription={setDescription} onFamily={setFamily} onSeverity={setSeverity} onDuration={setDuration} onRun={run} onCompare={compare} onBranch={branch}/><section className="center"><div className="metricstrip"><Metric n="Global pressure" v={frame.globalPressure}/><Metric n="Household strain" v={frame.globalHouseholdStrain}/><Metric n="Production" v={frame.globalProductionPressure}/><div><small>ACTIVE LINKS</small><strong>{frame.flows.length}</strong></div></div><WorldMap countries={frame.countries} flows={frame.flows} onSelect={select}/><div className="timeline panel"><div><b>WEEK {week}</b><span>{result.scenario.title}</span></div><input aria-label="Simulation week" type="range" min="0" max={result.frames.length-1} value={week} onChange={e=>setWeek(+e.target.value)}/><div className="ticks"><span>EVENT</span><span>PROPAGATION</span><span>ADAPTATION</span></div></div></section><aside className="panel analysis"><div className="tabs"><button className={tab==="world"?"active":""} onClick={()=>setTab("world")}><Activity/>World</button><button className={tab==="country"?"active":""} onClick={()=>setTab("country")}><Users/>Country</button><button className={tab==="compare"?"active":""} onClick={()=>setTab("compare")}><GitBranch/>Compare</button></div>{tab==="country"&&selected?<CountryInspector country={selected}/>:tab==="compare"?<ComparisonPanel current={result} baseline={baseline}/>:<WorldSummary top={top} analysis={result.analysis} select={select}/>}</aside></div></main>}
function Metric({n,v}:{n:string;v:number}){return <div><small>{n}</small><strong>{(v*100).toFixed(1)}%</strong></div>}
